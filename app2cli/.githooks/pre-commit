#!/bin/sh
# Pre-commit hook: runs lint, typecheck, and tests.
# To install: git config core.hooksPath .githooks

echo "Running pre-commit checks..."
make check || {
  echo "Pre-commit check failed. Fix issues before committing."
  exit 1
}
