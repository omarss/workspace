// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: models.sql

package store

import (
	"context"
)

const getActiveModelBySlug = `-- name: GetActiveModelBySlug :one
SELECT slug, display_name, provider, param_count_b, multiplier, active, created_at
FROM models
WHERE slug = $1
  AND active = true
`

func (q *Queries) GetActiveModelBySlug(ctx context.Context, slug string) (Model, error) {
	row := q.db.QueryRow(ctx, getActiveModelBySlug, slug)
	var i Model
	err := row.Scan(
		&i.Slug,
		&i.DisplayName,
		&i.Provider,
		&i.ParamCountB,
		&i.Multiplier,
		&i.Active,
		&i.CreatedAt,
	)
	return i, err
}

const getModelBySlug = `-- name: GetModelBySlug :one
SELECT slug, display_name, provider, param_count_b, multiplier, active, created_at
FROM models
WHERE slug = $1
`

func (q *Queries) GetModelBySlug(ctx context.Context, slug string) (Model, error) {
	row := q.db.QueryRow(ctx, getModelBySlug, slug)
	var i Model
	err := row.Scan(
		&i.Slug,
		&i.DisplayName,
		&i.Provider,
		&i.ParamCountB,
		&i.Multiplier,
		&i.Active,
		&i.CreatedAt,
	)
	return i, err
}

const listActiveModels = `-- name: ListActiveModels :many
SELECT slug, display_name, provider, param_count_b, multiplier, active, created_at
FROM models
WHERE active = true
ORDER BY param_count_b DESC
`

func (q *Queries) ListActiveModels(ctx context.Context) ([]Model, error) {
	rows, err := q.db.Query(ctx, listActiveModels)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Model
	for rows.Next() {
		var i Model
		if err := rows.Scan(
			&i.Slug,
			&i.DisplayName,
			&i.Provider,
			&i.ParamCountB,
			&i.Multiplier,
			&i.Active,
			&i.CreatedAt,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
