# /etc/systemd/system/ollama.service.d/override.conf
# Bind Ollama to all interfaces so k8s pods (10.42.0.0/24) can reach it.
# External access blocked by ufw default deny (port 11434 not allowed).
[Service]
Environment="OLLAMA_HOST=0.0.0.0"
