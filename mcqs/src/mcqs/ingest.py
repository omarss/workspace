"""Ingestion — filled in by task #2. Stub so `mcqs ingest` imports cleanly."""

from __future__ import annotations


def run(subject: str | None = None) -> None:
    raise NotImplementedError("ingest pipeline is implemented in task #2")
