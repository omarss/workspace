// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: leaderboards.sql

package store

import (
	"context"

	"github.com/google/uuid"
)

const improvementLeaderboard = `-- name: ImprovementLeaderboard :many
SELECT u.id,
       (COALESCE(NULLIF(u.nickname, ''), 'بدون اسم'))::text AS nickname,
       SUM(CASE WHEN a.answered_at >= date_trunc('week', now()) THEN 1 ELSE 0 END)::int
                                                    AS attempts_this_week,
       AVG(CASE WHEN a.answered_at >= date_trunc('week', now()) AND a.correct THEN 1.0
                WHEN a.answered_at >= date_trunc('week', now())                 THEN 0.0
                ELSE NULL END)::float8              AS this_week_accuracy,
       AVG(CASE WHEN a.answered_at >= date_trunc('week', now()) - interval '7 days'
                 AND a.answered_at <  date_trunc('week', now())
                 AND a.correct THEN 1.0
                WHEN a.answered_at >= date_trunc('week', now()) - interval '7 days'
                 AND a.answered_at <  date_trunc('week', now())
                THEN 0.0
                ELSE NULL END)::float8              AS last_week_accuracy
FROM users    u
JOIN attempts a ON a.user_id = u.id
WHERE u.leaderboard_opt_in = true
  AND a.correct IS NOT NULL
  AND a.answered_at >= date_trunc('week', now()) - interval '7 days'
GROUP BY u.id, u.nickname
HAVING SUM(CASE WHEN a.answered_at >= date_trunc('week', now()) THEN 1 ELSE 0 END) >= $1::int
ORDER BY (
    AVG(CASE WHEN a.answered_at >= date_trunc('week', now()) AND a.correct THEN 1.0
             WHEN a.answered_at >= date_trunc('week', now())                 THEN 0.0
             ELSE NULL END)
  - COALESCE(AVG(CASE WHEN a.answered_at >= date_trunc('week', now()) - interval '7 days'
                       AND a.answered_at <  date_trunc('week', now())
                       AND a.correct THEN 1.0
                      WHEN a.answered_at >= date_trunc('week', now()) - interval '7 days'
                       AND a.answered_at <  date_trunc('week', now())
                      THEN 0.0
                      ELSE NULL END), 0)
) DESC NULLS LAST
LIMIT $2
`

type ImprovementLeaderboardParams struct {
	MinAttempts int32 `json:"min_attempts"`
	LimitCount  int32 `json:"limit_count"`
}

type ImprovementLeaderboardRow struct {
	ID               uuid.UUID `json:"id"`
	Nickname         string    `json:"nickname"`
	AttemptsThisWeek int32     `json:"attempts_this_week"`
	ThisWeekAccuracy float64   `json:"this_week_accuracy"`
	LastWeekAccuracy float64   `json:"last_week_accuracy"`
}

// Week-over-week accuracy delta. Rewards growth, not raw skill — the
// safe-leaderboard goal from spec §22 rule 5. min_attempts gates noisy
// readings from very small samples.
func (q *Queries) ImprovementLeaderboard(ctx context.Context, arg ImprovementLeaderboardParams) ([]ImprovementLeaderboardRow, error) {
	rows, err := q.db.Query(ctx, improvementLeaderboard, arg.MinAttempts, arg.LimitCount)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []ImprovementLeaderboardRow
	for rows.Next() {
		var i ImprovementLeaderboardRow
		if err := rows.Scan(
			&i.ID,
			&i.Nickname,
			&i.AttemptsThisWeek,
			&i.ThisWeekAccuracy,
			&i.LastWeekAccuracy,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const masteryLeaderboard = `-- name: MasteryLeaderboard :many
SELECT u.id,
       (COALESCE(NULLIF(u.nickname, ''), 'بدون اسم'))::text   AS nickname,
       COUNT(*)::int                                  AS attempts,
       AVG(CASE WHEN a.correct THEN 1.0 ELSE 0.0 END) AS accuracy
FROM users    u
JOIN attempts a ON a.user_id = u.id
JOIN items    i ON i.id      = a.item_id
WHERE u.leaderboard_opt_in = true
  AND a.correct IS NOT NULL
  AND ($1::text IS NULL OR i.exam_type = $1)
GROUP BY u.id, u.nickname
HAVING COUNT(*) >= $2::int
ORDER BY accuracy DESC, attempts DESC
LIMIT $3
`

type MasteryLeaderboardParams struct {
	ExamType    *string `json:"exam_type"`
	MinAttempts int32   `json:"min_attempts"`
	LimitCount  int32   `json:"limit_count"`
}

type MasteryLeaderboardRow struct {
	ID       uuid.UUID `json:"id"`
	Nickname string    `json:"nickname"`
	Attempts int32     `json:"attempts"`
	Accuracy float64   `json:"accuracy"`
}

// Top users by accuracy. Restricted to opt-in users with at least
// min_attempts answered (spec §22 rule 8: don't rank tiny samples).
// Phone is never selected; nickname is the only public identifier.
func (q *Queries) MasteryLeaderboard(ctx context.Context, arg MasteryLeaderboardParams) ([]MasteryLeaderboardRow, error) {
	rows, err := q.db.Query(ctx, masteryLeaderboard, arg.ExamType, arg.MinAttempts, arg.LimitCount)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []MasteryLeaderboardRow
	for rows.Next() {
		var i MasteryLeaderboardRow
		if err := rows.Scan(
			&i.ID,
			&i.Nickname,
			&i.Attempts,
			&i.Accuracy,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const setLeaderboardOptIn = `-- name: SetLeaderboardOptIn :exec
UPDATE users
SET leaderboard_opt_in = $2,
    updated_at         = now()
WHERE id = $1
`

type SetLeaderboardOptInParams struct {
	ID               uuid.UUID `json:"id"`
	LeaderboardOptIn bool      `json:"leaderboard_opt_in"`
}

func (q *Queries) SetLeaderboardOptIn(ctx context.Context, arg SetLeaderboardOptInParams) error {
	_, err := q.db.Exec(ctx, setLeaderboardOptIn, arg.ID, arg.LeaderboardOptIn)
	return err
}

const setUserNickname = `-- name: SetUserNickname :exec
UPDATE users
SET nickname   = $2,
    updated_at = now()
WHERE id = $1
`

type SetUserNicknameParams struct {
	ID       uuid.UUID `json:"id"`
	Nickname string    `json:"nickname"`
}

func (q *Queries) SetUserNickname(ctx context.Context, arg SetUserNicknameParams) error {
	_, err := q.db.Exec(ctx, setUserNickname, arg.ID, arg.Nickname)
	return err
}
